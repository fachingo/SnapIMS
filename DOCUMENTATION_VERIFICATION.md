# SnapIMS v0.15.0 Documentation Verification

- Active guides identify v0.15.0 and inventory schema 16.
- Publish documentation follows the final UI: Validate → Simulate → Create Drafts → Review → Publish Live.
- Exact confirmations `SUBMIT`, `SUBMIT LIVE`, `RESTORE`, `ARCHIVE`, `DELETE`, `KEEP SHOPIFY`, and `MERGE` match the application.
- Publication selection and `read_publications`/`write_publications` requirements are documented.
- Product and batch management wording matches current buttons and scopes.
- Recovery documents describe durable Shopify jobs and schema-16 backups.
- Current screenshots are from `browser-evidence/v0.15.0/`.
- DOCX guides were rendered to page images and visually inspected.
- Generated PDFs were rendered and visually inspected.
- A final browser walkthrough was compared against guide terminology before packaging.
